export type SfxKind='reveal'|'correct'|'wrong'|'next';let ctx:AudioContext|null=null;const spec:Record<SfxKind,{freq:number;end:number;gain:number;second?:number}>={reveal:{freq:660,end:.11,gain:.032,second:880},correct:{freq:880,end:.09,gain:.035,second:1175},wrong:{freq:220,end:.1,gain:.024},next:{freq:520,end:.065,gain:.02,second:700}};export function playSfx(kind:SfxKind,enabled=true){if(!enabled||typeof window==='undefined')return;try{ctx=ctx||new AudioContext();const now=ctx.currentTime,o=ctx.createOscillator(),g=ctx.createGain(),s=spec[kind];o.type=kind==='wrong'?'triangle':'sine';o.frequency.setValueAtTime(s.freq,now);if(s.second)o.frequency.exponentialRampToValueAtTime(s.second,now+s.end);g.gain.setValueAtTime(.0001,now);g.gain.exponentialRampToValueAtTime(s.gain,now+.012);g.gain.exponentialRampToValueAtTime(.0001,now+s.end);o.connect(g);g.connect(ctx.destination);o.start(now);o.stop(now+s.end+.01)}catch{}}
