import {useEffect,useState} from 'react';import {ChevronRight,Mic2,Volume2} from 'lucide-react';import {Story} from './content';import {buildPlayPlan,PlayClipId} from './playPlan';type ClipState='ready'|'pending'|'failed';type RecallResult='good'|'close'|'miss';export type Weakness='meaning'|'reply'|'register'|'transfer'|'recall';export default function PracticeLane({story,clipStatus,playClip,onComplete,onSfx,onMistake}:{story:Story;clipStatus:Partial<Record<PlayClipId,ClipState>>;playClip:(storyId:string,clipId:PlayClipId,rate?:number)=>Promise<boolean>;onComplete:(result:RecallResult)=>void;onSfx:(kind:'correct'|'wrong'|'next')=>void;onMistake:(kind:Weakness)=>void}){const plan=buildPlayPlan(story);const[step,setStep]=useState(0);const[answer,setAnswer]=useState<number|null>(null);const[heard,setHeard]=useState(false);const[busy,setBusy]=useState<PlayClipId|null>(null);const[said,setSaid]=useState(false);const[slow,setSlow]=useState(false);const[scenarioIndex,setScenarioIndex]=useState(0);const[scenarioShown,setScenarioShown]=useState(false);const[scenarioHeard,setScenarioHeard]=useState(false);useEffect(()=>{setStep(0);setAnswer(null);setHeard(false);setBusy(null);setSaid(false);setSlow(false);setScenarioIndex(0);setScenarioShown(false);setScenarioHeard(false)},[story.id]);if(!plan)return <section className='play-empty'>这个主题还没有完整玩法。</section>;const ready=(id:PlayClipId)=>clipStatus[id]==='ready',rate=slow?.82:1;const play=async(id:PlayClipId,mark=false)=>{if(!ready(id)||busy)return false;setBusy(id);const ok=await playClip(story.id,id,rate);setBusy(null);if(ok&&mark)setHeard(true);return ok};const auto=(id:PlayClipId)=>{if(ready(id))void play(id)};const next=()=>{const n=step+1;if(n>6)return;onSfx('next');setStep(n);setAnswer(null);setHeard(false);setScenarioShown(false);setScenarioHeard(false);if(n===1)auto('replyPrompt');if(n===5){setScenarioIndex(0);if(ready('scene0'))void play('scene0',true)}};const registerData=step===2?{label:'平时怎么说',emoji:'🙂',cue:'对象是朋友、熟人。先听“短、快、自然”的版本。',prompt:'哪个最像平时真实会话？',choices:[{text:plan.daily,id:'daily' as PlayClipId},{text:plan.business,id:'business' as PlayClipId},{text:plan.polite,id:'polite' as PlayClipId}],correct:0,correctClip:'daily' as PlayClipId,feedback:`日常：${plan.daily}`} :step===3?{label:'敬语怎么说',emoji:'🙇',cue:'对象换成不熟的人、店员、同事。重点是关系和距离，不只是句尾。',prompt:'哪个版本更适合礼貌场景？',choices:[{text:plan.business,id:'business' as PlayClipId},{text:plan.polite,id:'polite' as PlayClipId},{text:plan.daily,id:'daily' as PlayClipId}],correct:1,correctClip:'polite' as PlayClipId,feedback:`礼貌：${plan.polite}`}:{label:'商务日语怎么说',emoji:'💼',cue:'对象换成客户、上司、正式说明。日语经常会整句换表达，而不是把原句机械敬语化。',prompt:'同一个交际意图，正式场合最自然的是哪个？',choices:[{text:plan.polite,id:'polite' as PlayClipId},{text:plan.daily,id:'daily' as PlayClipId},{text:plan.business,id:'business' as PlayClipId}],correct:2,correctClip:'business' as PlayClipId,feedback:`${plan.businessNote}｜${plan.business}`};const chooseRegister=(i:number)=>{if(answer!==null)return;onSfx(i===registerData.correct?'correct':'wrong');if(i!==registerData.correct)onMistake('register');setAnswer(i);auto(registerData.correctClip)};const choice=(choices:string[],correct:number,feedback:string,mistake:Weakness,onPick?:(i:number)=>void)=><><div className='play-choices'>{choices.map((c,i)=><button key={`${i}-${c}`} className={answer===i?(i===correct?'correct':'wrong'):''} onClick={()=>{if(answer!==null)return;onSfx(i===correct?'correct':'wrong');if(i!==correct)onMistake(mistake);setAnswer(i);onPick?.(i)}}>{c}</button>)}</div>{answer!==null&&<div className='play-feedback'><strong>{answer===correct?'对，就是这个感觉':'把错误点记下来，马上听/看正确版本'}</strong><small>{feedback}</small></div>}</>;const top=<><div className='play-progress'>{[0,1,2,3,4,5,6].map(i=><i key={i} className={i<=step?'active':''}/>)}</div><div className='play-step-title'><span>{step+1}/7</span><div className='play-title-actions'><button className={slow?'active':''} onClick={()=>setSlow(v=>!v)}>0.8×</button><strong>{step===0?'听一句猜意思':step===1?'现场怎么回':step===2?'平时怎么说':step===3?'敬语怎么说':step===4?'商务日语怎么说':step===5?'换个情景':'自己说一遍'}</strong></div></div></>;if(step===0)return <section className='core-play'>{top}<div className='play-emoji'>{plan.scenarios[0].emoji}</div><p className='play-cue'>{plan.scenarios[0].cue}</p><h1>这句话在这个情景里最接近什么意思？</h1>{ready('listen')&&!heard?<button className='listen-first' disabled={!!busy} onClick={()=>void play('listen',true)}><Volume2 size={22}/><span>{busy==='listen'?'播放中…':'先听，不看文字'}</span></button>:<><strong className='scene-line'>{plan.scenarios[0].jp}</strong>{clipStatus.listen!=='ready'&&<small className='audio-pending'>声音准备中 · 先用文字玩，不报错</small>}</>}{(!ready('listen')||heard)&&choice([plan.scenarios[0].cn,plan.scenarios[1].cn,plan.scenarios[2].cn],0,`${plan.scenarios[0].jp} · ${plan.scenarios[0].cn}`,'meaning')}{answer!==null&&<div className='after-audio'><button disabled={!ready('listen')||!!busy} onClick={()=>void play('listen')}><Volume2 size={16}/> 再听一次</button><button className='next-audio' onClick={next}>下一玩法 <ChevronRight size={17}/></button></div>}</section>;if(step===1)return <section className='core-play'>{top}<div className='play-emoji'>💬</div><p className='play-cue'>先听对方说什么，再决定你本人怎么回。</p><div className='inline-listen'>{ready('replyPrompt')?<button disabled={!!busy} onClick={()=>void play('replyPrompt')}><Volume2 size={17}/> 再听对方说</button>:<small>对方语音准备中</small>}</div><h1>{story.use.prompt}</h1>{choice(story.use.choices,story.use.correct,story.use.feedback,'reply',()=>auto('reply'))}{answer!==null&&<div className='after-audio'><button disabled={!ready('reply')||!!busy} onClick={()=>void play('reply')}><Volume2 size={16}/> 听正确回应</button><button className='next-audio' onClick={next}>下一玩法 <ChevronRight size={17}/></button></div>}</section>;if(step>=2&&step<=4)return <section className='core-play'>{top}<div className='play-emoji'>{registerData.emoji}</div><p className='play-cue'>{registerData.cue}</p><h1>{registerData.prompt}</h1><div className='register-list'>{registerData.choices.map((c,i)=><div className={`register-row ${answer===i?(i===registerData.correct?'correct':'wrong'):''}`} key={`${i}-${c.text}`}><button className='register-answer' onClick={()=>chooseRegister(i)} disabled={answer!==null}>{c.text}</button>{ready(c.id)&&<button className='register-audio' aria-label={`播放 ${c.text}`} disabled={!!busy} onClick={()=>void play(c.id)}><Volume2 size={17}/></button>}</div>)}</div>{answer!==null&&<div className='play-feedback'><strong>{answer===registerData.correct?'对，身份、关系和表达方式对上了':'这里不是单纯“更敬语”，先抓住交际意图'}</strong><small>{registerData.feedback}</small></div>}{answer!==null&&<div className='after-audio'><button disabled={!ready(registerData.correctClip)||!!busy} onClick={()=>void play(registerData.correctClip)}><Volume2 size={16}/> 再听正确版本</button><button className='next-audio' onClick={next}>下一玩法 <ChevronRight size={17}/></button></div>}</section>;if(step===5){const scene=plan.scenarios[scenarioIndex],id=`scene${scenarioIndex}` as PlayClipId,isAudioOnly=scenarioIndex===3&&ready(id);const goScene=()=>{if(scenarioIndex<plan.scenarios.length-1){onSfx('next');setScenarioIndex(v=>v+1);setScenarioShown(false);setScenarioHeard(false)}else next()};const reveal=(gaveUp=false)=>{if(gaveUp)onMistake('transfer');onSfx(gaveUp?'wrong':'next');setScenarioShown(true);if(!gaveUp&&ready(id))auto(id)};return <section className='core-play'>{top}<div className='scenario-counter'>{scenarioIndex+1}/{plan.scenarios.length}</div><div className='play-emoji'>{scene.emoji}</div><p className='play-cue'>{scenarioIndex===0?scene.cue:scenarioIndex===1?`${scene.cue}｜有中文提示`:scenarioIndex===2?`${scene.cue}｜不看中文`:scenarioIndex===3?'只听一次，不看日文；听完以后自己复述或回应。':`${scene.cue}｜最后一个，不先听参考`}</p><h1>{scenarioIndex===0?'同一个语言点，换到这里你还能跟上吗？':scenarioIndex===1?'看提示，先自己说一句':scenarioIndex===2?'只看场景，先自己组织日语':scenarioIndex===3?'只靠耳朵，听完自己说':'不听参考，直接把日语叫出来'}</h1>{scenarioIndex===0&&!scenarioShown&&(ready(id)?<button className='listen-first' disabled={!!busy} onClick={async()=>{const ok=await play(id);if(ok)setScenarioShown(true)}}><Volume2 size={22}/><span>{busy===id?'播放中…':'先听这个新情景'}</span></button>:<button className='text-reveal' onClick={()=>setScenarioShown(true)}>音频准备中 · 看文字</button>)}{scenarioIndex>0&&!scenarioShown&&<>{scenarioIndex===1&&<small className='audio-pending'>中文提示：{scene.cn}</small>}{isAudioOnly&&!scenarioHeard?<button className='listen-first' disabled={!!busy} onClick={async()=>{const ok=await play(id);if(ok)setScenarioHeard(true)}}><Volume2 size={22}/><span>{busy===id?'播放中…':'只听一次'}</span></button>:<div className='speak-actions'><button className='speak-first' disabled={isAudioOnly&&!scenarioHeard} onClick={()=>reveal(false)}><Mic2 size={18}/> 我说好了</button><button onClick={()=>reveal(true)}>想不出，揭晓</button></div>}{scenarioIndex===3&&!ready(id)&&<small className='audio-pending'>这段声音还在准备，先按场景自己说。</small>}</>}{scenarioShown&&<div className='scenario-reveal'><strong>{scene.jp}</strong><small>{scene.cn}</small>{ready(id)&&<button onClick={()=>void play(id)}><Volume2 size={16}/> 听参考</button>}<button className='next-audio' onClick={goScene}>{scenarioIndex<plan.scenarios.length-1?'下一个情景':'最后一关'} <ChevronRight size={17}/></button></div>}</section>}return <section className='core-play'>{top}<div className='play-emoji'>🎙️</div><p className='play-cue'>{story.review.prompt}</p><h1>{story.review.cloze}</h1>{!said?<button className='speak-first' onClick={()=>{onSfx('next');setSaid(true);auto('recall')}}><Mic2 size={19}/> 我说好了</button>:<div className='speak-stage'><strong>{plan.clips.recall.text}</strong><small>{story.review.feedback}</small>{ready('recall')?<button className='reference-audio' disabled={!!busy} onClick={()=>void play('recall')}><Volume2 size={17}/> 听参考答案</button>:<small className='audio-pending'>参考音频准备中</small>}<div className='speak-actions three'><button onClick={()=>onComplete('miss')}>完全不会</button><button onClick={()=>onComplete('close')}>有点不同</button><button onClick={()=>onComplete('good')}>差不多</button></div></div>}</section>}
