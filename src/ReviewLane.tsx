import {useState} from 'react';import {Mic2,Volume2} from 'lucide-react';import {Story} from './content';import {buildPlayPlan,PlayClipId} from './playPlan';import type {Weakness} from './PracticeLane';type ClipState='ready'|'pending'|'failed';type RecallResult='good'|'close'|'miss';type MemoryRecord={strength:number;nextReviewAt:number;lastSeen:number;weaknesses?:Partial<Record<Weakness,number>>};export default function ReviewLane({story,clipStatus,playClip,memory,onComplete,onMistake}:{story:Story;clipStatus:Partial<Record<PlayClipId,ClipState>>;playClip:(storyId:string,clipId:PlayClipId,rate?:number)=>Promise<boolean>;memory:MemoryRecord;onComplete:(result:RecallResult)=>void;onMistake:(kind:Weakness)=>void}){const plan=buildPlayPlan(story);const[revealed,setRevealed]=useState(false);const[heard,setHeard]=useState(false);const[busy,setBusy]=useState<PlayClipId|null>(null);if(!plan)return <section className='play-empty'>这个主题还没有完整复习玩法。</section>;const ready=(id:PlayClipId)=>clipStatus[id]==='ready';const ranked=(Object.entries(memory.weaknesses||{}) as [Weakness,number][]).filter(([,v])=>v>0).sort((a,b)=>b[1]-a[1]),weakness=ranked[0]?.[0],sceneIndex=Math.min(4,Math.max(1,memory.strength)),scene=plan.scenarios[sceneIndex],sceneId=`scene${sceneIndex}` as PlayClipId,variant=weakness==='register'?'register':((weakness==='meaning'||memory.strength<=1)&&ready(sceneId))?'audio':memory.strength>=4?'open':'scene',label=variant==='audio'?'听力辨认':variant==='register'?'语域换说法':variant==='scene'?'场景召回':'开放输出',targetClip=variant==='register'?'business' as PlayClipId:sceneId,targetText=variant==='register'?plan.business:scene.jp;const play=async(id:PlayClipId)=>{if(!ready(id)||busy)return false;setBusy(id);const ok=await playClip(story.id,id,1);setBusy(null);return ok};const rate=(result:RecallResult)=>{if(result!=='good')onMistake(variant==='register'?'register':variant==='audio'?'meaning':variant==='scene'?'transfer':'recall');onComplete(result)};return <section className='core-play'><div className='play-step-title'><span>复习</span><div className='play-title-actions'><strong>{label}</strong></div></div><div className='play-emoji'>{variant==='register'?'💼':scene.emoji}</div>{variant==='audio'&&!revealed&&<><p className='play-cue'>这次只靠耳朵，不先给文字。</p><h1>听完以后，先在脑中说出它在这个场景里的意思。</h1>{!heard?<button className='listen-first' disabled={!!busy} onClick={async()=>{const ok=await play(sceneId);if(ok)setHeard(true)}}><Volume2 size={22}/><span>{busy===sceneId?'播放中…':'只听一次'}</span></button>:<div className='speak-actions'><button onClick={()=>setRevealed(true)}>我已经想好了</button><button onClick={()=>setRevealed(true)}>想不出，揭晓</button></div>}</>}{variant==='register'&&!revealed&&<><p className='play-cue'>对象换成客户、上司或正式说明。先想“这个意图日本人会怎么自然表达”。</p><h1>不要机械加敬语，先自己说一个正式版本。</h1><div className='speak-actions'><button className='speak-first' onClick={()=>setRevealed(true)}><Mic2 size={18}/> 我说好了</button><button onClick={()=>setRevealed(true)}>想不出，揭晓</button></div></>}{(variant==='scene'||variant==='open')&&!revealed&&<><p className='play-cue'>{scene.cue}</p>{variant==='scene'&&<small className='audio-pending'>提示：{scene.cn}</small>}<h1>{variant==='scene'?'先自己组织一句日语':'不看中文、不先听参考，直接说出来'}</h1><div className='speak-actions'><button className='speak-first' onClick={()=>setRevealed(true)}><Mic2 size={18}/> 我说好了</button><button onClick={()=>setRevealed(true)}>想不出，揭晓</button></div></>}{revealed&&<div className='speak-stage'><strong>{targetText}</strong><small>{variant==='register'?plan.businessNote:scene.cn}</small>{ready(targetClip)?<button className='reference-audio' disabled={!!busy} onClick={()=>void play(targetClip)}><Volume2 size={17}/> 听参考</button>:<small className='audio-pending'>参考音频准备中 · 先用文字核对</small>}<div className='speak-actions three'><button onClick={()=>rate('miss')}>完全不会</button><button onClick={()=>rate('close')}>有点不同</button><button onClick={()=>rate('good')}>说出来了</button></div></div>}</section>}
