name: Exploration Visual V7 Ground Balance
on:
  push:
    branches: [nihongo-visual-v7-ground-balance-20260906]
    paths:
      - '.github/workflows/explore-visual-v7-ground-balance.yml'
      - 'scripts/visual-v7-ground-balance-patch.py'
      - 'scripts/visual-v3-browser.mjs'
      - 'src/explore/cinematic-v3.css'
  workflow_dispatch:
permissions:
  contents: write
concurrency:
  group: exploration-visual-v7-ground-balance
  cancel-in-progress: false
jobs:
  ground-balance:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        shell: bash --noprofile --norc -e -o pipefail {0}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
      - name: Apply road shadow-balance patch
        run: |
          python3 scripts/visual-v7-ground-balance-patch.py
          git add src/explore/world.ts
          if ! git diff --cached --quiet; then
            git config user.name 'github-actions[bot]'
            git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
            git commit -m 'Lift asphalt shadow detail without flattening the scene'
            git push origin HEAD:nihongo-visual-v7-ground-balance-20260906
          fi
      - name: Game-only source gates
        run: |
          npm ci --no-audit --no-fund
          npm run typecheck
          npm test -- src/explore/explore.test.ts
          npm run build
      - name: Desktop visual and movement evidence
        run: |
          npm install --no-save --package-lock=false --no-audit --no-fund playwright
          npx playwright install --with-deps chromium
          npm run preview -- --host 127.0.0.1 --port 4173 >/tmp/preview.log 2>&1 &
          for i in $(seq 1 30); do curl -fsS http://127.0.0.1:4173/explore.html >/dev/null && break; sleep 1; done
          mkdir -p /tmp/visual-v7
          EXPLORE_ARTIFACTS=/tmp/visual-v7 node scripts/visual-v3-browser.mjs
      - name: Upload balanced-ground evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: explore-visual-v7-${{ github.run_number }}
          path: /tmp/visual-v7/
          retention-days: 14
