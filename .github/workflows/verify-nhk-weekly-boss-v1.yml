name: Verify NHK weekly Boss v1

on:
  push:
    branches: [nihongo-weekly-boss-v1-20260901]
    paths:
      - '.github/workflows/verify-nhk-weekly-boss-v1.yml'

permissions:
  contents: write

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: nihongo-weekly-boss-v1-20260901
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
      - name: Integrate weekly Boss entry and styling
        run: python scripts/patch-nhk-weekly-boss-v1.py
      - run: npm install --no-audit --no-fund
      - run: npm run typecheck
      - run: npm test
      - run: npm run build
      - name: Commit verified bounded source changes
        run: |
          set -euxo pipefail
          git config user.name "Sol Supervisor"
          git config user.email "actions@users.noreply.github.com"
          git add \
            src/NhkBossPage.tsx \
            src/nhkBoss.ts \
            src/nhkBoss.test.ts \
            src/NhkMorningPage.tsx \
            src/nhkMorning.css
          git diff --cached --check
          git commit -m "Add weekly spoken NHK Boss"
          git push origin HEAD:nihongo-weekly-boss-v1-20260901
